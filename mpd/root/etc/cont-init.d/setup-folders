#!/usr/bin/with-contenv bashio
set -e

bashio::log.info  'Setup Folders'

MUSIC_FOLDER=$(bashio::config 'music_folder')
PLAYLIST_FOLDER=$(bashio::config 'playlist_folder')

mkdir -p /data/database \
    "${MUSIC_FOLDER}" \
    "${PLAYLIST_FOLDER}"
